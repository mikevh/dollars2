/** Shared by BudgetGroupCard's header row and LineItemRow so columns line up. */
export const GROUP_GRID_COLUMNS = 'minmax(140px,1fr) minmax(110px,150px) minmax(120px,170px)'

/** Which figure the per-group dropdown shows in the third money column. */
export type GroupMetric = 'remaining' | 'actual'
